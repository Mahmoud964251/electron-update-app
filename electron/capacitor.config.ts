import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ma.electron.update.app',
  appName: 'electron-update-app',
  webDir: 'www'
};

export default config;
